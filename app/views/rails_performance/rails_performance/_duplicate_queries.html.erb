<table class="table is-fullwidth is-hoverable is-narrow is-size-7">
  <thead>
    <tr>
      <th data-sort="int">Span</th>
      <th data-sort="string">Query</th>
    </tr>
  </thead>
  <tbody>
    <% @data.each do |query, values| %>
      <tr>
          <td class="nowrap has-text-right">
            <%= values.size %>
          </td>
          <td>
            <%= query %>
          </td>
      </tr>
    <% end %>

    <% if @data.empty? %>
      <tr>
        <td>Nothing to show here. Perphaps details about the requests already expired. Setting "slow_requests_time_window" should be equal to "duration".</td>
      </td>
    <% end %>
  </tbody>
</table>
